# Example .env for docker-compose (DO NOT commit real credentials)
# Copy this to .env and fill in real values locally.

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=elearning_db

# Optional: set to expose DB port to host for local dev. Leave commented or unset in CI/production.
# HOST_DB_PORT=5432
